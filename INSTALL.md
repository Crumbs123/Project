# 🚀 Быстрая установка и запуск

## 1️⃣ Установка базы данных

### Запустите MySQL и выполните команды:

```sql
-- Создание базы данных
mysql -u root -p < meat_production_db.sql

-- Добавление рецептов (ВАЖНО!)
mysql -u root -p MeatProductionDB < recipes_setup.sql
```

✅ После выполнения `recipes_setup.sql`:
- Удалено "Сало украинское" из продукции
- Добавлены рецепты для всех копченых изделий
- Установлены правильные пропорции сырья

## 2️⃣ Настройка подключения

Откройте файл: `Classes/DatabaseHelper.cs`

Измените строку подключения:
```csharp
private static string connectionString = 
    "Server=localhost;Database=MeatProductionDB;Uid=root;Pwd=ВАШ_ПАРОЛЬ;";
```

## 3️⃣ Установка пакетов NuGet

Откройте проект в Visual Studio или выполните:

```bash
cd MeatProductionApp_Complete
dotnet restore
```

### Требуемые пакеты:
- ✅ MySql.Data (8.0.33) - уже в проекте
- ✅ DocX (2.4.0) - уже добавлен в .csproj

## 4️⃣ Запуск приложения

### Через Visual Studio:
1. Откройте `MeatProductionApp.csproj`
2. Нажмите F5 или "Запуск"

### Через командную строку:
```bash
dotnet run
```

## 5️⃣ Вход в систему

### Администратор:
- Логин: `admin`
- Пароль: `admin123`

### Менеджер:
- Логин: `manager`
- Пароль: `manager123`

---

## ✨ Что нового в этой версии?

### 🎯 Умное производство с рецептами
Теперь при создании производства:
1. Выбираете продукт → автоматически загружается рецепт
2. Указываете количество → система пересчитывает нужное сырье
3. Видите цветную индикацию наличия:
   - 🟢 Зеленый = все есть
   - 🔴 Красный = не хватает
4. Сырье списывается **автоматически** по рецепту!

**Больше никаких ошибок вроде "сделать сало из специй"!** 🎉

### 📊 Три типа отчетов Word

Нажмите кнопку **"📄 Экспорт в Word"** и выберите:

1. **Детальный отчет**
   - Все записи производства
   - Полная статистика

2. **Сводный отчет**
   - По каждому виду продукции
   - ТОП-5 продуктов

3. **Отчет по рецептам**
   - Все рецепты с ингредиентами
   - Остатки на складе с цветами:
     - 🔴 Критический
     - 🟠 Низкий
     - 🟢 Нормальный

---

## 🐛 Если что-то не работает

### Проблема: "Не могу подключиться к БД"
**Решение:**
1. Проверьте, запущен ли MySQL
2. Проверьте пароль в `DatabaseHelper.cs`
3. Убедитесь, что база `MeatProductionDB` создана

### Проблема: "Нет рецептов в производстве"
**Решение:**
1. Убедитесь, что выполнен `recipes_setup.sql`
2. Проверьте таблицу `Recipes` в БД:
   ```sql
   SELECT COUNT(*) FROM Recipes;
   ```
   Должно быть минимум 12 записей

### Проблема: "Ошибка при создании Word отчета"
**Решение:**
1. Установите пакет DocX:
   ```bash
   dotnet add package DocX --version 2.4.0
   ```
2. Перезапустите приложение

### Проблема: "Есть 'Сало украинское' в списке"
**Решение:**
Выполните вручную:
```sql
USE MeatProductionDB;
DELETE FROM Products WHERE ProductName LIKE '%Сало украинское%';
```

---

## 📸 Как это работает?

### Пример: Производство грудинки копченой

1. **Открываем производство** → выбираем "Грудинка копченая"
2. **Система загружает рецепт:**
   - Свинина (грудинка): 120 кг
   - Соль: 2.5 кг
   - Специи: 0.5 кг
3. **Вводим количество:** 50 кг
4. **Система пересчитывает** (коэффициент 0.5):
   - Свинина: 60 кг
   - Соль: 1.25 кг
   - Специи: 0.25 кг
5. **Проверяет наличие** на складе
6. **При сохранении:**
   - ✅ Списывается 60 кг свинины
   - ✅ Списывается 1.25 кг соли
   - ✅ Списывается 0.25 кг специй
   - ✅ Добавляется 50 кг грудинки на склад

**Все автоматически! Никаких ошибок!** 🎯

---

## 💡 Полезные советы

1. **Перед производством** проверьте остатки сырья
2. **Используйте фильтр по датам** для быстрого поиска
3. **Создавайте отчеты регулярно** для контроля
4. **Следите за цветовой индикацией** в отчетах

---

## 📞 Нужна помощь?

1. Откройте `README.md` для подробной документации
2. Проверьте раздел "Устранение неполадок"
3. Посмотрите логи в таблице `ActivityLogs`

**Удачи! 🚀**
